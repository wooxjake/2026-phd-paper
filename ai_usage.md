# AI 모델 선택 가이드 — 논문 작업용

## 결론

한 모델로 고정하기보다 작업별로 나누는 편이 정확도와 크레딧 효율을 모두 잡습니다.

| 작업                         | 1순위                                  | 경제형 대안        | 이유                                                                                                          |
| ---------------------------- | -------------------------------------- | ------------------ | ------------------------------------------------------------------------------------------------------------- |
| 논문 검색·원문 대조          | `GPT-5.6 Sol` 또는 `Claude Opus 4.8`   | `GPT-5.4`          | 긴 PDF·추출 텍스트에서 방법, 표본, 장비, 결과를 대조하는 능력이 중요합니다.                                   |
| 문헌 요약·references.md 등록 | `GPT-5.6 Sol`                          | `GPT-5.4`          | 정해 둔 항목(표본, 절차, 장비, URL 등)을 빠뜨리지 않고 구조화하는 작업에 적합합니다.                          |
| 서론·이론적 배경의 문맥 정리 | `Claude Opus 4.8` 또는 `GPT-5.6 Sol`   | `Claude Sonnet 5`  | 긴 문단의 논리 흐름, 연구 공백, 주장 강도, 문체 정리에 강합니다.                                              |
| 방법·통계·심사위원 피드백    | `GPT-5.6 Sol` 또는 `Claude Opus 4.8`   | `GPT-5.4`          | 반복측정, 선형혼합모형(linear mixed model, 선형혼합모형), 효과크기, 신호처리 판단에 고성능 모델이 필요합니다. |
| Markdown 정리·파일 재배치    | `GPT-5.4 mini` 또는 `Claude Haiku 4.5` | `GPT-5 mini`       | 내용 판단보다 서식·구조 정리가 중심이므로 저비용 모델로 충분합니다.                                           |
| 간단한 용어 통일·체크리스트  | `GPT-5.4 mini`                         | `Claude Haiku 4.5` | 명확한 규칙을 반복 적용하는 작업입니다.                                                                       |
| 코드·PDF 추출 스크립트       | `GPT-5.3-Codex`                        | `GPT-5.4`          | Python 스크립트, 테스트, 파일 처리에는 Codex 계열이 더 직접적입니다.                                          |

### 작업별로 우선 볼 열

| 작업                        | 우선 볼 열                     | 이유                              |
| --------------------------- | ------------------------------ | --------------------------------- |
| PDF 여러 편과 참고문헌 대조 | 컨텍스트 크기, `In`, 캐시 읽기 | 읽을 자료가 많음                  |
| 서론·논의 문단 새 작성      | `Out`, 모델 성능               | 논리적 출력 품질이 중요           |
| references.md 상태 갱신     | `In`, 캐시 읽기                | 반복 대조 작업이 많음             |
| Markdown 목차·서식 정리     | `Out`, 낮은 `In`               | 편집 작업 비중이 큼               |
| 스캔 표·그래프 확인         | 비전, 컨텍스트 크기            | 이미지·표 인식 필요               |
| 연구방법·통계 검토          | 모델 성능, 컨텍스트 크기       | 설계·변인·분석계획을 함께 봐야 함 |

## 추천 조합과 저장소 설정

| 역할                | 모델              | 용도                                                                  |
| ------------------- | ----------------- | --------------------------------------------------------------------- |
| 기본 작성 모델      | `GPT-5.6 Sol`     | 논문 초안, 문헌 원문 대조, references.md 구조화, 연구방법·통계 검토   |
| 문체·논리 집중 모델 | `Claude Opus 4.8` | 서론·논의의 문맥 연결, 심사위원 관점 비판, 여러 문헌의 논증 구조 비교 |
| 저비용 실행 모델    | `GPT-5.4 mini`    | Markdown 서식, 목차 재배치, 중복 표현 탐지, 용어·인용 일관성 점검     |
| 코드·추출 스크립트  | `GPT-5.3-Codex`   | PDF 추출·번역 스크립트 작성·점검                                      |

- `phd-thesis-writer` 에이전트: 기본 모델 `GPT-5.6 Sol` 또는 `Claude Opus 4.8`
- `phd-thesis-feedback` 에이전트: `GPT-5.6 Sol` 우선, 문체·논증 중심 검토는 `Claude Opus 4.8`

비용은 높지만 잘못된 인용·통계·방법론 판단의 수정 비용이 더 크므로, 고성능 모델은 핵심 판단이 필요한 작업에만 사용합니다. 저비용 모델은 서식·구조 정리 같은 "문서 정리 비서" 역할에 사용합니다.

## 피하는 편이 좋은 선택

- `GPT-5 mini`, `GPT-5.4 mini`, `Claude Haiku 4.5`만으로 문헌의 결과나 통계적 적합성을 확정하는 방식
- `Gemini 3.5 Flash`를 핵심 근거 판정에 단독 사용하는 방식
- 어떤 모델이든 검색 결과 또는 AI 생성 서지정보를 원문 검증 없이 references.md에 넣는 방식

정확도는 모델 등급보다도 저장소 규칙이 더 크게 좌우합니다. references.md 등록 전에 PDF·추출 텍스트·공식 HTML 원문을 대조하는 절차를 유지하는 것이 가장 중요합니다.

---

## 모델 비교표 항목 설명

VS Code 모델 목록의 각 행은 모델 하나이며, 오른쪽 열은 해당 모델의 기능과 크레딧 소모량입니다.

### 컨텍스트 크기

컨텍스트 크기(context window, 문맥 창)는 AI가 한 번에 참고할 수 있는 최대 텍스트 양입니다.

- `1M`(약 100만 토큰): 긴 PDF 여러 편, references.md, outline.md, introduce.md, 정책 파일을 함께 읽고 비교하는 작업에 유리합니다. 논문처럼 앞부분의 연구목적과 뒷부분의 방법·결과가 연결되는지 확인할 때 중요합니다.
- `160K`~`553K`: 한두 개 문서 검토에는 충분하지만, 참고문헌과 여러 원문 PDF를 한 번에 대조할 때는 앞부분 문맥이 빠질 수 있습니다.

비유하면 컨텍스트 크기는 **책상 크기**입니다. 다만 책상이 크다고 항상 정확도가 높아지는 것은 아니며, 담을 수 있는 자료의 양이 늘어날 뿐입니다.

### 기능: 도구

`도구`는 AI가 VS Code 안에서 파일을 읽고, 검색하고, 필요하면 수정하며, 웹 자료를 확인할 수 있다는 뜻입니다. 다음 작업에 필요합니다.

- introduce.md, outline.md, references.md 대조
- references와 하위 폴더의 PDF·TXT·번역본 확인
- 본문 인용과 참고문헌 상태(`candidate`, `cited`) 비교
- Markdown 주석 삽입, 문헌 검색, 통계·PDF 추출 스크립트 점검

저장소 규칙이 많은 프로젝트에서는 `도구`가 있는 모델을 고르는 것이 실용적입니다.

### 기능: 비전

`비전`은 이미지, 캡처, 표, 스캔 PDF를 읽는 기능입니다. 스캔 논문의 표·그림·캡션 확인, 이미지형 PDF의 통계표 추출 보조, 그래프 축·단위·유의성 표기 검토, 화면 캡처 해석에 유용합니다. 다만 비전은 **처음 내용을 파악하는 보조 수단**으로만 쓰고, 통계 수치·표본 수·장비 모델·DOI는 PDF 내장 텍스트나 공식 HTML 원문으로 다시 대조해야 합니다.

### 비용: In / Out

- `In`(입력 토큰): 사용자의 요청, 열린 파일, AI가 읽은 introduce.md·references.md·정책 파일, PDF 추출 텍스트, 이전 대화 문맥, 에이전트 지침이 모두 포함됩니다. 읽을 자료가 많은 논문 작업에서는 `In` 비용이 중요합니다.
- `Out`(출력 토큰): 서론 작성, 논문 번역, 대규모 인라인 주석, 다수의 참고문헌 항목 등록, 긴 피드백 보고서처럼 출력이 많은 작업에서 비용이 커집니다. 짧은 지적 요청은 `Out` 비용 부담이 작습니다.

### 캐시 읽기 / 캐시 쓰기

- 캐시 읽기(cache read): 이미 읽은 긴 자료(CLAUDE.md, 에이전트 파일, 정책 파일, references.md 등)를 다시 사용할 때의 비용입니다. 비용이 낮을수록 같은 논문 파일을 반복 검토할 때 경제적입니다.
- 캐시 쓰기(cache write): 긴 문맥을 이후 요청에도 활용할 수 있게 준비하는 비용입니다. 처음 큰 자료를 읽을 때 발생하며, 이후 같은 세션에서는 캐시 읽기로 전환되어 효율이 좋아질 수 있습니다.
